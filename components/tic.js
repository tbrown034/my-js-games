//selectors

const ticTurn = document.getElementById("tic-turn");
const ticBoard = document.getElementById("tic-board");
const ticWinner = document.getElementById("tic-winner");
const ticImage = document.getElementById("tic-image");
const playTic = document.getElementById("play-tic");

// functions

const startGame = () => {
  // Hide ticImage, show Name
};

const createBoard = () => {
  // Hide ticImage, show Name
};

// helper functions

// event listerns

playTic.addEventListener("click", startGame);

//initializers
